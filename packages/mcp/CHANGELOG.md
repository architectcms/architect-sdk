# @architectcms/mcp

## 1.1.0

### Minor Changes

- 176994b: Publish the Architect CMS MCP server as a standalone npm package (moved from the private architect repo). Adds a `bin` so it runs via `npx -y @architectcms/mcp`.
