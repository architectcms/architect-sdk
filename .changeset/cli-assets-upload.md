---
"@architectcms/cli": minor
---

Add `architect assets upload <file>` to upload media assets (with `--title`, `--description`, `--tags`, `--alt`, `--mime-type`), wrapping the SDK's management asset upload.
